image: ruby:2.5.3

variables:
  JEKYLL_ENV: production

pages:
  before_script:
  - cd docs
  stage: deploy
  script:
  - bundle install
  - bundle exec jekyll build -d public
  artifacts:
    paths:
    - public
  only:
  - master
